# 隐藏应用支持添加快捷方式

## 需求概述

支持给隐藏的应用添加快捷方式，这种情况下用户可以仅禁用某个应用，且可以通过快捷方式启用并打开该应用。


## 修改点
1. 应用选择列表页：在用户在应用列表选择隐藏应用的时候，如果选择隐藏了某一个应用，那在这个应用的下面新增一栏小字以及一个复选框，表示要不要给这个应用创建快捷方式。注意是只有先选择了隐藏应用的条件下，再出现下面这行小字以及复选框。
   - 复选框：默认不选中
   - 小字：“创建快捷方式”
   - 复选框选中后，会创建一个该应用的快捷方式，图标就直接取这个应用的图标名称也是这个应用的名称。功能是先取消隐藏这个应用，然后再打开该应用。
   - 本地记录：当前已经创建快捷方式的应用列表存到preference里面，逻辑参考隐藏应用列表的那个存储方式
2. 锁屏后自动隐藏应用：目前有一个功能，用户可以配置在锁屏后 x 分钟，自动隐藏所有应用，但是只在用户“取消隐藏了所有应用”时才生效。帮我添加以下几种情况：
   - 用户通过创建的快捷方式打开应用，应用会自动取消隐藏
   - 应用更新后，会自动取消隐藏
   针对以上两种自动取消隐藏的情况，需要在代码里面添加相应的处理，确保锁屏后自动隐藏的功能能覆盖到这些情况。
