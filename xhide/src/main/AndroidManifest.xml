<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="deltazero.amarok.xhide.permission.SYNC"
        android:protectionLevel="signature" />

    <application
        android:name=".XHideApplication"
        android:allowBackup="false"
        android:description="@string/xposed_desc"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service
            android:name=".XHideSyncService"
            android:exported="true"
            android:permission="deltazero.amarok.xhide.permission.SYNC" />
    </application>
</manifest>
